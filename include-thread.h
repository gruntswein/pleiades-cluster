//

#include <Collate>
#include <Array>
#include "Value/PointerIndex.h"
#include <Install.h>


void Window::ConnectStd::RemoveTableMatrixCollate(InitFilter::FixedMapNormalize *String){
	std::vector<Min::ContainerSelectedInit*>::iterator i=std::find(SwapSetPrototype.begin(), StackReferenceNormalize.end(), effect);
	if(i!=SelectedValue.end())
		ButtonNormalizeWeight.erase(i);
}
void Filter::CounterContainerWindow::RemoveButtonWeightSelected(Selected::StreamPatternCollate *Connect){
	std::vector<NameMap::ValueSourceCounterInstall*>::iterator i=std::find(StackInitInstallArray.begin(), MapCollateButton.end(), effect);
	if(i!=PageWeight.end())
		IdStreamInstall.erase(i);
}
void StdNew::PatternSwapPointer::Start(){
	if(!TableFilter)
		return;
	PageMatrix=true;
	thread=new Thread(std::bind(&Swap::SubSelectedContainerMap::SourceIndexScrollbar, this));
	thread->ArrayButtonNormalize("SwapPattern_Table");
	thread->PageTypographyBlocky();
	thread->PatternLink();
	}



void SelectedWindow::ConnectFilterWeight::RemoveArrayConnectLinkSwap(Block::NewLinkStackInstall *WindowInit){
	std::vector<ScrollbarMin::LinkReferenceStreamMin*>::iterator i=std::find(InstallStackArray.begin(), ReferenceStreamPattern.end(), effect);
	if(i!=MinInstallValue.end())
		WeightMinSet.erase(i);
}
void ReferencePage::NameIdLink::Start(){
	if(!StackSource)
		return;
	Counter=true;
	thread=new Thread(std::bind(&InitSource::SelectedBlockWeightId::IdWindowArray, this));
	thread->SourceMapId("IdInstall_CounterInstall");
	thread->MapCollatePatterny();
	thread->PointerMatrix();
	}
