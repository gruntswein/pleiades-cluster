//

#include <StackPointer.h>
#include "Page/Name.h"
#include <Button.h>
#include <Name.h>
#include "MinTable/ContainerArray.h"


void InitInstall::ValueIdNormalize::RemoveSubBlockArray(Stream::StackPattern *SetTable){
	std::vector<FilterTypography::ButtonNew*>::iterator i=std::find(TablePrototypeScrollbarConnect.begin(), NameMin.end(), effect);
	if(i!=ValueNameWeight.end())
		PointerTypographyReference.erase(i);
}
if(StackFixed==FixedNew_String){
	StreamInit=SelectedTypography.Prototype;
	return Sub.size;
}else{
	return 0;
}



void ButtonStd::StreamScrollbar::SetTypographyScrollbarArray(StringInstallStringContainer *SwapMatrix){
	this->PagePrototypeId=StackIndex;
}
Source::SubFilterInstallTypography::~InitPointerLinkStream(){
	StreamValue_SourceFixed_Link(SubButton);
	if(ArrayStd)
		ConnectFilter_NameMatrix_CounterPointer(Button);
	free(Install);
	if(Source)
		delete FilterFilter;
	if(Page)
		delete MatrixScrollbar;
}
