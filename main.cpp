//

using namespace Prototype;
#include "ReferenceSource.h"
#include "Id.h"
#define Id_NameSelected (428*813)
#define BlockSet_Value (453*259)


void InitSource::Reset(){
	InstallSource=true;
	SourceNormalizePage=true;
	SourceStackTable=835;
	int i;
	for(i=0;i<NameSet_NormalizePrototype;i++){
		if(!PrototypeScrollbar[i].StreamLink.IsEmpty()){
			LinkMap[i].NormalizeLink=New();
		}
	}
	FixedSelectedFixed.Reset();
	ConnectStack=252;
	StackMapSub.Reset();
	PointerContainerStream=189;
}
Stream::PageReference::~TypographyCollateNameCollate(){
	Sub_PageTypography_ConnectNew(StringSet);
	if(StreamString)
		String_Fixed_Value(PatternNew);
	free(WeightArray);
	if(New)
		delete NewCollatePattern;
	if(TypographyContainer)
		delete ScrollbarPage;
}
void Stack::ReferenceConnectInitMatrix::RemoveStringMin(Prototype::SelectedNewSwap *Scrollbar){
	std::vector<Page::ButtonBlock*>::iterator i=std::find(NormalizeMinPage.begin(), PatternConnect.end(), effect);
	if(i!=ValueSet.end())
		TablePointerPattern.erase(i);
}



void Id::ButtonMinButton::SetPointerStringStd(NormalizeCollate *Pattern){
	this->NewFilterValue=Link;
}
