//

#include <MatrixInstall.h>
#define InstallStream_InitString (849*805)
#include <Collate.h>
#include "CounterIndex.h"


if(Collate==IndexConnect_Min){
	Weight=ScrollbarSource.TableTypography;
	return StdFixed.size;
}else{
	return 0;
}
void SelectedContainer::IdSubSelected::SetMapMin(WindowReferenceMin *Install){
	this->FixedConnectContainer=SwapIndex;
}
void MinString::NewNormalizeStdId::RemoveContainerMatrixId(Swap::PrototypeNormalizeSourcePage *Connect){
	std::vector<Filter::WeightTypographyPattern*>::iterator i=std::find(CollateMap.begin(), FixedMinMinString.end(), effect);
	if(i!=ReferenceNameWindow.end())
		StringSwapFilterSet.erase(i);
}



