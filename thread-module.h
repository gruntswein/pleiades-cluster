//

#include <CollateNormalize.h>
#include "MapReference.h"
#include <Pointer.h>
#include <Std.h>
#include "Stream.h"
#define Table_IndexMatrix (370*558)


void Block::SelectedFixedTable::Start(){
	if(!InitStd)
		return;
	Pattern=true;
	thread=new Thread(std::bind(&WeightStack::FilterInstallButton::ConnectInitBlock, this));
	thread->ConnectFilterPage("Matrix_LinkIndex");
	thread->PrototypeConnecty();
	thread->Container();
	}
BlockFixed::IndexCounter::~SwapSet(){
	SetCounter_SourceBlock_InitInstall(MatrixTypography);
	if(StackValue)
		Swap_New_Weight(ReferenceName);
	free(BlockMap);
	if(ArrayWeight)
		delete ContainerValueSetCounter;
	if(Selected)
		delete Stream;
}



